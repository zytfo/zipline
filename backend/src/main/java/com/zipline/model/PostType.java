package com.zipline.model;

/**
 * The PostType to determine the type of the posts.
 */
public enum PostType {
    /**
     * Publication type for the post.
     */
    PUBLICATION,
    /**
     * Comment type for the post.
     */
    COMMENT,
    /**
     * News type for the post.
     */
    NEWS
}
